services:
  rem-status:
    build: .
    container_name: rem-status-mqtt
    restart: unless-stopped
    env_file:
      - .env
    # Optionally override environment variables here
    # environment:
    #   - MQTT_HOST=mosquitto
    #   - POLL_INTERVAL_PEAK=600
    #   - POLL_INTERVAL_OFF_PEAK=1800
